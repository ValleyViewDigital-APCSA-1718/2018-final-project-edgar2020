import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)
public class crate extends destroyable
{
    public crate()
    {   
        GreenfootImage box = getImage();
        box.scale(35, 35);
        setImage(box);
    }// crate constructor, resizes image
}//end of crate constructor












